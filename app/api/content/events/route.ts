import { NextRequest, NextResponse } from "next/server";
import { recordContentEvent } from "@/lib/content-store";
export async function POST(request: NextRequest) { try { const body = await request.json() as { contentId?: string; assetId?: string; eventType?: string; visitorId?: string; path?: string; referrer?: string }; if (!body.contentId || !body.eventType) return NextResponse.json({ error: "contentId and eventType are required" }, { status: 400 }); await recordContentEvent({ contentId: body.contentId, eventType: body.eventType, assetId: body.assetId, visitorId: body.visitorId, path: body.path, referrer: body.referrer, userAgent: request.headers.get("user-agent") || undefined }); return NextResponse.json({ success: true }); } catch { return NextResponse.json({ error: "Unable to record event" }, { status: 400 }); } }
