"use client";
import { useEffect, useState } from "react";
import { useParams, useRouter } from "next/navigation";
import Link from "next/link";
type Form = { title: string; slug: string; type: string; category: string; excerpt: string; body: string; status: string; featured: boolean };
export default function EditContentPage() { const { id } = useParams<{ id: string }>(); const router = useRouter(); const [form, setForm] = useState<Form | null>(null); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); useEffect(() => { void fetch(`/api/admin/content/${id}`).then(async (r) => { const d = await r.json(); if (!r.ok) throw new Error(d.error); setForm({ title: d.item.title, slug: d.item.slug, type: d.item.type, category: d.item.category || "", excerpt: d.item.excerpt || "", body: d.item.body || "", status: d.item.status, featured: d.item.featured }); }).catch((e) => setError(e.message)); }, [id]); if (error) return <p className="p-8 text-red-700">{error}</p>; if (!form) return <p className="p-8 text-gray-500">Loading...</p>; const update = (key: keyof Form, value: string | boolean) => setForm({ ...form, [key]: value } as Form); const save = async (e: React.FormEvent) => { e.preventDefault(); setSaving(true); const r = await fetch(`/api/admin/content/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); setSaving(false); if (!r.ok) { const d = await r.json(); return setError(d.error || "Save failed"); } router.push("/admin/content"); }; return <main className="min-h-screen bg-gray-50"><header className="border-b bg-white"><div className="mx-auto flex max-w-4xl justify-between px-6 py-5"><h1 className="text-xl font-bold">Edit content</h1><Link href="/admin/content" className="text-sm text-gray-600">Back to library</Link></div></header><form onSubmit={save} className="mx-auto max-w-4xl space-y-6 px-6 py-8"><section className="rounded-lg bg-white p-7 shadow-sm"><label className="mb-2 block text-sm font-medium">Title</label><input required value={form.title} onChange={(e) => update("title", e.target.value)} className="mb-5 w-full rounded border px-3 py-2" /><div className="grid gap-5 md:grid-cols-2"><input required value={form.slug} onChange={(e) => update("slug", e.target.value)} className="w-full rounded border px-3 py-2" /><select value={form.type} onChange={(e) => update("type", e.target.value)} className="w-full rounded border px-3 py-2">{["GUIDE","INSPIRATION","JOURNAL","CASE_STUDY","LOOKBOOK","DOWNLOAD"].map((x) => <option key={x}>{x}</option>)}</select></div></section><section className="rounded-lg bg-white p-7 shadow-sm"><textarea value={form.excerpt} onChange={(e) => update("excerpt", e.target.value)} rows={3} className="mb-5 w-full rounded border px-3 py-2" placeholder="Excerpt" /><textarea value={form.body} onChange={(e) => update("body", e.target.value)} rows={14} className="w-full rounded border px-3 py-2 font-mono text-sm" placeholder="Body" /></section><section className="rounded-lg bg-white p-7 shadow-sm"><input value={form.category} onChange={(e) => update("category", e.target.value)} className="w-full rounded border px-3 py-2" placeholder="Category" /><div className="mt-5 flex gap-6 text-sm"><label><input type="checkbox" checked={form.featured} onChange={(e) => update("featured", e.target.checked)} /> Featured</label><label>Status <select value={form.status} onChange={(e) => update("status", e.target.value)} className="ml-2 rounded border px-2 py-1"><option>DRAFT</option><option>PUBLISHED</option><option>ARCHIVED</option></select></label></div></section><button disabled={saving} className="rounded bg-[#19382f] px-6 py-3 text-sm font-semibold text-white">{saving ? "Saving..." : "Save changes"}</button></form></main>; }
