"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
type Item = { id: string; slug: string; title: string; excerpt: string | null; type: string; image: string | null; document: string | null };
export function FeaturedContentSection() { const [items, setItems] = useState<Item[]>([]); useEffect(() => { void fetch("/api/content").then(async (r) => { if (r.ok) setItems((await r.json()).items || []); }); }, []); if (!items.length) return null; const featured = items.slice(0, 3); return <section aria-labelledby="ideas-heading" className="bg-[var(--paper)] px-6 py-16 sm:px-10 lg:px-14 xl:px-16"><div className="mx-auto max-w-[2100px]"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-[var(--moss)]">From the journal</p><h2 id="ideas-heading" className="mt-3 font-[family-name:var(--font-display)] text-5xl">Ideas for outside.</h2></div><Link href="/ideas" className="text-[10px] font-bold uppercase tracking-[0.16em] text-[var(--forest)]/70 hover:text-[var(--forest)]">View all ideas <span aria-hidden="true" className="ml-2">→</span></Link></div>{featured.length === 1 ? <Link href={`/ideas/${featured[0].slug}`} className="group mt-8 grid overflow-hidden border border-[var(--forest)]/15 bg-[#eeebe4] lg:grid-cols-[.82fr_1.18fr]"><div className="flex min-h-[360px] flex-col justify-between p-7 sm:p-10 lg:min-h-[460px]"><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-[var(--moss)]">{featured[0].type.replace("_", " ")}</p><h3 className="mt-4 max-w-xl font-[family-name:var(--font-display)] text-3xl leading-[1.02] sm:text-5xl">{featured[0].title}</h3>{featured[0].excerpt && <p className="mt-5 line-clamp-4 max-w-lg text-sm leading-6 text-[var(--ink)]/72">{featured[0].excerpt}</p>}</div><span className="mt-10 inline-flex items-center gap-3 text-[10px] font-bold uppercase tracking-[0.16em]">Explore the catalogue <span aria-hidden="true">→</span></span></div><div className="relative min-h-[360px] overflow-hidden bg-[#d8d2c6] lg:min-h-[460px]">{featured[0].document ? <div className="pointer-events-none absolute inset-0 overflow-hidden"><iframe src={`${featured[0].document}#page=1&view=FitH`} title="Catalogue preview" tabIndex={-1} className="absolute -top-12 left-0 h-[calc(100%+60px)] w-full border-0" /></div> : featured[0].image ? <img src={featured[0].image} alt="" className="absolute inset-0 h-full w-full object-cover" /> : <div className="absolute inset-0 bg-[linear-gradient(135deg,#d2c3a4,#f3eee4)]" />}</div></Link> : <div className="mt-8 grid gap-x-6 gap-y-8 sm:grid-cols-2 lg:grid-cols-3">{featured.map((item) => <Link key={item.id} href={`/ideas/${item.slug}`} className="group border-t border-[var(--forest)]/20 pt-4"><h3 className="font-[family-name:var(--font-display)] text-2xl leading-tight group-hover:underline">{item.title}</h3>{item.excerpt && <p className="mt-2 max-w-md text-sm leading-6 text-[var(--ink)]/70">{item.excerpt}</p>}</Link>)}</div>}</div></section>; }
