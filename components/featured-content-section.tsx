"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
type Item = { id: string; slug: string; title: string; excerpt: string | null; type: string; image: string | null };
export function FeaturedContentSection() { const [items, setItems] = useState<Item[]>([]); useEffect(() => { void fetch("/api/content").then(async (r) => { if (r.ok) setItems((await r.json()).items || []); }); }, []); if (!items.length) return null; return <section aria-labelledby="ideas-heading" className="bg-[var(--paper)] px-6 py-16 sm:px-10 lg:px-14 xl:px-16"><div className="mx-auto max-w-[2100px]"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-[var(--moss)]">From the journal</p><h2 id="ideas-heading" className="mt-3 font-[family-name:var(--font-display)] text-5xl">Ideas for outside.</h2></div><Link href="/ideas" className="text-[10px] font-bold uppercase tracking-[0.16em] text-[var(--forest)]/70 hover:text-[var(--forest)]">View all ideas <span aria-hidden="true" className="ml-2">→</span></Link></div><div className="mt-8 grid gap-x-6 gap-y-8 sm:grid-cols-2 lg:grid-cols-3">{items.slice(0, 3).map((item) => <Link key={item.id} href={`/ideas/${item.slug}`} className="group border-t border-[var(--forest)]/20 pt-4"><h3 className="font-[family-name:var(--font-display)] text-2xl leading-tight group-hover:underline">{item.title}</h3>{item.excerpt && <p className="mt-2 max-w-md text-sm leading-6 text-[var(--ink)]/70">{item.excerpt}</p>}</Link>)}</div></div></section>; }
